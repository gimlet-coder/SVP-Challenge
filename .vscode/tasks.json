{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "g++: Build Project (SVP-Challenge)",
            "command": "mingw32-make", 
            "args": [], 
            "options": {
                "cwd": "${workspaceFolder}",
                "env": {
                    // MinGW の bin フォルダへのパスを追加
                    "PATH": "C:/msys64/mingw64/bin:${env:PATH}"
                }
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Compiler: mingw32-make (プロジェクト全体のビルド)"
        }
    ]
}